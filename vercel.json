{
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "npx tsc --build --force && cd artifacts/nutter-xmd && BASE_PATH=/ pnpm run build",
  "outputDirectory": "artifacts/nutter-xmd/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
