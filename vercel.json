{
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "npx tsc --build --force && BASE_PATH=/ pnpm --filter @workspace/nutter-xmd run build && node -e \"require('fs').cpSync('artifacts/nutter-xmd/dist','dist',{recursive:true,force:true})\"",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
